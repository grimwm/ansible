---
- name: Ensure winget is available
  ansible.windows.win_command: winget --version
  register: nvim_winget_version
  changed_when: false

- name: Install Neovim (via winget)
  community.windows.win_winget:
    name: Neovim.Neovim
    state: present
    exact: true
